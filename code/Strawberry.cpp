/*
 * PANCAKE CLASS
 * Copyright © 2020+ Mariana Moreira
 */

#include "Strawberry.hpp"

using namespace basics;
using namespace std;

namespace flip
{

    Strawberry::Strawberry(Atlas * atlas) : Food(atlas)
    {/*
        // Adds the corresponding sprites for the ascending movement
        ascending_keyframes.push_back (ID(strawberry_up_1));
        /ascending_keyframes.push_back (ID(strawberry_up_2));
        ascending_keyframes.push_back (ID(strawberry_up_3));

        // Adds the corresponding sprites for the falling movement
        falling_keyframes.push_back (ID(strawberry_down_1));
        falling_keyframes.push_back (ID(strawberry_down_2));
        falling_keyframes.push_back (ID(strawberry_down_3));*/
    }

}
